<section class="final-cta">
    <div class="final-cta__box">
        <h2 class="final-cta__title">{{ __('Ready to secure your sales in Europe?') }}</h2>
        <p class="final-cta__text">{{ __('Get compliant in 24h with Festilaw.') }}</p>
        <div class="final-cta__actions">
            <a href="{{ route('get-started.index') }}" class="btn btn--coral btn--lg">{{ __('Get my mandate now') }}</a>
            <a href="{{ route('contact') }}" class="btn btn--outline-light btn--lg">{{ __('Talk to us') }}</a>
        </div>
    </div>
</section>
